// 클래스 선언
class 클래스이름 {

}

// 클래스를 이용한 객체 생성
- new 클래스이름()
- const 인스턴스 = new 클래스이름()

// private 속성 : 클래스 내부에서만 접근 가능한 속성
#속성이름

// getter, setter
- getter : 속성 값을 확인할 때 사용하는 메서드
- setter : 속성 값을 지정할 때 사용하는 메서드
```
class 클래스이름 {
  get 속성이름 () { return 속성값}
  set 속성이름 (지정할속성값) {}
}
```

// static 속성과 메서드